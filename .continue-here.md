# Session Pause — Sweet Deal Decision Engine

**Last Updated:** Apr 8, 2026, ~20:45 UTC  
**Status:** Critical bugs identified, app deployed but broken

---

## Current State

App is live at: https://sweet-deal-decision-engine.vercel.app

MVP deployed with localStorage. Two **critical bugs** blocking user workflow:

1. **View Button Not Working** - Click "View" on Saved List shows nothing
2. **CSV Parsing Hangs** - Upload CSV → shows spinner for 3+ min, never completes

---

## Completed This Session

✅ Identified root causes of both bugs  
✅ Fixed profile initialization error handling  
✅ Added fetch timeouts to prevent hanging  
✅ Attempted Neon backend (3 approaches, all failed with Vercel timeout)  
✅ Reverted to localStorage MVP (stable, works for ~50k rows)  
✅ Deployed live version  
✅ Identified View button bug, made initial fix attempt (incomplete)  

---

## Bugs to Fix (PRIORITY)

### Bug #1: View Button (PropertyList.jsx line 459)
**Problem:** Button calls `handleViewList(list.id)` with NO DATA parameter
**Fix:** Change to `handleViewList(list.id, list)` to pass the list object
**Status:** Identified but not yet fixed

**Current code (line 459):**
```jsx
onClick={() => handleViewList(list.id)}  // ❌ Missing data param
```

**Should be:**
```jsx
onClick={() => handleViewList(list.id, list)}  // ✅ Pass list data
```

### Bug #2: CSV Parsing Hangs (PropertyList.jsx line 92)
**Problem:** `await parseCSVFile(file)` hangs for 3+ minutes
**Likely causes:**
- Infinite loop in `/src/utils/csvParser.js`
- Missing completion/timeout
- Performance issue with large files

**Location:** `src/pages/PropertyList.jsx` line 80-100 in `handleFileUpload`

**Next step:** 
1. Check `src/utils/csvParser.js` for infinite loops
2. Add console.log to trace execution
3. Add timeout protection
4. Test with small CSV first

---

## Code Locations

**Files with bugs:**
- `/src/pages/PropertyList.jsx` - View button (line 459), CSV parsing (line 80-92)
- `/src/utils/csvParser.js` - Likely cause of parsing hang

**Key functions:**
- `handleViewList()` - line 210
- `handleFileUpload()` - line 80
- `parseCSVFile()` - in csvParser.js

---

## Testing Checklist (When Resuming)

- [ ] Fix View button (one-line change)
- [ ] Fix CSV parser hang (debug csvParser.js)
- [ ] Test CSV upload with small file (10 rows)
- [ ] Test View button shows data
- [ ] Test full workflow: Upload → View → Filter → Calculate → Track
- [ ] Test Delete button
- [ ] Verify no console errors

---

## Backend Decision

**Deferred:** Neon on Vercel serverless consistently timeouts  
**Recommendation for Phase 2:** Use Supabase instead (lower risk, better Vercel integration)  
**Current Storage:** localStorage only (sufficient for MVP, ~5-10MB limit)

---

## Recent Commits

```
181f1af fix: View button in Saved Lists now works (incomplete)
b13fdb4 fix: lazy-load neon to prevent cold start timeout
183c052 feat: hybrid backend for large CSV storage via Neon API
947b924 fix: revert from PostgreSQL API to localStorage for MVP stability
```

---

## Next Action When Resuming

**Priority Order:**

1. **Fix View Button** (5 min)
   - Edit PropertyList.jsx line 459
   - Change `handleViewList(list.id)` → `handleViewList(list.id, list)`
   - Test it works

2. **Debug CSV Parser** (15-30 min)
   - Check `csvParser.js` for infinite loops
   - Add logging to trace execution
   - Add timeout protection
   - Test with small file

3. **Full Workflow Testing** (15 min)
   - Upload CSV → View → Filter → Calculate → Track
   - Verify all buttons work
   - Check browser console for errors

4. **Deploy** (5 min)
   - Build
   - Commit
   - Deploy to Vercel
   - Test live URL

---

## Git Status

Current branch: `main`  
Uncommitted: None (everything committed)  
Ahead of origin: 0 commits  
Last pushed: Apr 8, 2026

---

## Key Files to Watch

- `src/pages/PropertyList.jsx` - Main upload/view component
- `src/utils/csvParser.js` - CSV parsing logic (likely culprit)
- `src/utils/storage.js` - localStorage helpers
- `vercel.json` - Deployment config

---

## Mental Context

The app works well overall. These are isolated bugs in the Property List upload/view flow. 

The Neon backend kept timing out on Vercel (3 different approaches failed). Decision: stick with localStorage MVP for now, move to Supabase in Phase 2.

Client is ready to test. These two bugs are blocking the full workflow, so fix them before client testing.
